# Render Blueprint for the Angular frontend
# https://render.com/docs/blueprint-spec
services:
  - type: static
    name: training-helper-fe
    buildCommand: npm install && npm run build
    staticPublishPath: dist/training-helper-fe/browser
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

