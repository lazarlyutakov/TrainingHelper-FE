# Render Blueprint for the Angular frontend
# https://render.com/docs/blueprint-spec
services:
  - type: static
    name: training-helper-fe
    buildCommand: npm run build:prod
    staticPublishPath: dist/app
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

